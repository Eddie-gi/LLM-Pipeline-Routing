question: What is the main difference between in-network computing (INC) and edge computing? option 1: INC enables flexible distribution of computation tasks across the network, while edge computing is done on edge servers outside the network option 2: INC and edge computing both enable flexible distribution of computation tasks across the network, but edge computing is done on edge servers inside the network option 3: INC and edge computing both enable computation outside the network, but INC is done on edge servers inside the network option 4: INC and edge computing both enable computation inside the network, but INC is done on edge servers outside the network 